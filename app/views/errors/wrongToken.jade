extends ../layouts/insearch

block content
	params = array('title' => §('global.title'))
	h1=§('global.wrong-token.title', $params)

	include ../includes/alert
	+alert('danger', 'global.wrong-token.description')
